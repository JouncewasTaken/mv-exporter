# ============================================================================
# forms.example.txt — TEMPLATE ONLY. Copy to "forms.txt" and replace the
# contents with the forms YOUR organization actually uses.
#
# Format: one Multiview form code per line. Blank lines and lines starting
# with "#" are ignored. Everything after a "#" on a line is a comment.
#
# How to find your form codes: every Multiview instance exposes its own set
# of forms (they differ by which modules your org licenses/uses). Pull your
# catalog and pick the forms you want to archive — do not assume the examples
# below match your tenant. The exporter resolves each form's structure at
# runtime and skips (loudly) any form it cannot profile.
# ============================================================================

# The examples below are only to show the SYNTAX. Replace them.
# (AP / GL / AR exist on most Multiview installs, but confirm against your own.)

VOUCHER_F1          # example: AP vouchers header
ENTRY_F1            # example: GL journal entry header
AR_INQUIRY_F1       # example: AR transactions

# Add the detail/line and any other forms your org uses, e.g.:
# VOUCHER_DIST_INQUIRY_F1   # AP distribution lines (if you use them)
# ENTRY_DETAIL_F1           # GL journal lines
